Code文件夹是该实验的代码清单
其中6个python文件与实验报告中优化的版本迭代所对应
test1文件夹为后来为每种类别添加了5张图片后的扩展测试集，以避免数据偶然性
（注：由于企业微信邮件附件不允许超过50MB，故只把test1文件夹放在了压缩包中）
1.py中采用两个卷积层，一个全连接层与SGD优化器
2.py中采用三个卷积层和SGD优化器
3.py中在1.py的基础上添加droppout层
4.py在3.py的基础上将优化器由SGD更换为Adam
5.py中在4.py的基础上增加了数据增强
6.py在4.py的基础上添加了一个全连接层
如果运行存在问题，需要检查路径有无修改为本地路径
Result文件夹中只存储了本次实验的最终结果，所有迭代版本的结果，详见实验报告。